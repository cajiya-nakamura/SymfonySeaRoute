{% extends 'base.html.twig' %}

{% block title %}Edit Route{% endblock %}

{% block body %}
    <h1>Edit Route</h1>

    {{ include('route/_form.html.twig', {'button_label': '更新'}) }}

    <a href="{{ path('app_route_index') }}" class="btn btn-outline-secondary mb-2">一覧</a>

    {{ include('route/_delete_form.html.twig') }}
{% endblock %}
