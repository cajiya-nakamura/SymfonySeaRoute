{% extends 'base.html.twig' %}

{% block title %}Vehicle{% endblock %}

{% block body %}
    <h1>Vehicle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehicle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vehicle.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vehicle_index') }}" class="btn btn-outline-secondary mb-2">一覧</a>

    <a href="{{ path('app_vehicle_edit', {'id': vehicle.id}) }}" class="btn btn-outline-secondary mb-2">編集</a>

    {{ include('vehicle/_delete_form.html.twig') }}
{% endblock %}
